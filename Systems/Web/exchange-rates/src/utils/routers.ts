import defaultUri from "./defaultRoute";


export const Routes = {
    ExchangeRate: `${defaultUri}8020/api/exchange/exchange-rate`,
    ExchangeRates: `${defaultUri}8020/api/exchange/exchange-rates`
}

export default Routes;